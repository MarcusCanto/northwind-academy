version: 2

models:
  - name: fct_sales
    description: 
      Tabela com as vendas da Northwind.
    columns:
      - name: sk_sales
        description: Chave primária da fatos vendas.
        tests:
          - unique
          - not_null
